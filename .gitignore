# Environment files with secrets
.env.dev
.env.prod
.env
# Keep .env.example tracked as a template
!.env.example

# MCP settings (contains DB credentials)
.kiro/settings/

# GSD agent configs (contain DB credentials)
.kiro/agents/gsd*.json